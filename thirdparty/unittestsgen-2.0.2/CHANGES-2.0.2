1. Corrected problem in JDK-1.5 where it is possible
   to have many methods with the same set of parameters
   but with different return type.
   In such case test methods were generated incorrectly
   - there were generated many identical test methods.
